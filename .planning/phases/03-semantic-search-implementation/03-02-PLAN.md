---
phase: 03-semantic-search-implementation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/search/search_engine.py
  - tests/test_search_engine.py
autonomous: true
requirements: [DOC-01, DOC-02]

must_haves:
  truths:
    - "用户可以浏览所有文章列表"
    - "用户可以按主题分类查看文章"
    - "文章按主题分组（TikTok、AI、财务等）"
  artifacts:
    - path: "src/search/search_engine.py"
      provides: "Document listing and topic categorization methods"
      contains: "list_all_documents"
    - path: "tests/test_search_engine.py"
      provides: "Tests for document browsing"
      contains: "test_list_all_documents"
  key_links:
    - from: "src/search/search_engine.py"
      to: "src/storage/document_store.py"
      via: "DocumentStore.list_documents()"
      pattern: "document_store\\.list_documents"
---

<objective>
实现文档浏览和主题分类功能

Purpose: 让用户能够浏览所有知识库文章，并按主题（TikTok、AI、财务等）分类查看
Output: 文档列表和主题分类方法，支持按主题筛选文章
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-semantic-search-implementation/03-RESEARCH.md
@.planning/phases/01-data-layer-foundation/01-03-SUMMARY.md
</context>

<interfaces>
<!-- Key types from existing codebase -->

From src/storage/document_store.py:
```python
class DocumentStore:
    def __init__(self, base_path: str = "/Users/a1234/wiki/raw/articles")
    
    def list_documents(self) -> List[Path]:
        """List all document files (sorted by filename)"""
        # Returns Path objects for .md and .xlsx files
    
    def get_document_metadata(self, file_path: Path) -> Dict[str, Any]:
        """Extract metadata from document file"""
        # Returns: {
        #   "path": str,
        #   "name": str,
        #   "extension": str,
        #   "size_bytes": int,
        #   "modified_timestamp": float,
        #   "relative_path": str
        # }
    
    def get_document_count(self) -> Dict[str, int]:
        """Get count by type"""
        # Returns: {"markdown": int, "excel": int, "total": int}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 实现文档列表和主题分类</name>
  <files>
    src/search/search_engine.py
    tests/test_search_engine.py
  </files>
  
  <behavior>
    - list_all_documents() 返回所有文档的元数据列表
    - 每个文档包含：id、title、file_type、topic、size、modified_date
    - list_documents_by_topic() 返回按主题分组的文档字典
    - 主题从文件名自动提取（TikTok、AI、财务、收款、流量、广告、其他）
    - 文档按文件名排序
  </behavior>
  
  <action>
在 SearchEngine 类中添加文档浏览方法：

**1. 初始化时添加 DocumentStore：**

在 `__init__` 方法中：
```python
from src.storage.document_store import DocumentStore

def __init__(self, vector_store: Optional[HenryVectorStore] = None):
    # ... existing vector store initialization ...
    
    # Add document store for browsing
    self.document_store = DocumentStore()
```

**2. 添加 list_all_documents() 方法：**

```python
def list_all_documents(self) -> List[Dict[str, Any]]:
    """
    列出所有文档及其元数据
    
    Returns:
        List of document metadata dicts with keys:
        - id: str (file path)
        - title: str (filename without extension)
        - file_type: str ('markdown' or 'excel')
        - topic: str (extracted from filename)
        - size_bytes: int
        - modified_date: str (ISO format)
        - relative_path: str
    """
    documents = self.document_store.list_documents()
    
    result = []
    for doc_path in documents:
        metadata = self.document_store.get_document_metadata(doc_path)
        
        # Extract topic from filename
        topic = self._extract_topic_from_filename(doc_path.name)
        
        # Convert to user-friendly format
        result.append({
            'id': str(doc_path),
            'title': doc_path.stem,  # filename without extension
            'file_type': 'markdown' if doc_path.suffix == '.md' else 'excel',
            'topic': topic,
            'size_bytes': metadata['size_bytes'],
            'modified_date': self._format_timestamp(metadata['modified_timestamp']),
            'relative_path': metadata['relative_path']
        })
    
    return result
```

**3. 添加 list_documents_by_topic() 方法：**

```python
def list_documents_by_topic(self) -> Dict[str, List[Dict[str, Any]]]:
    """
    按主题分组列出文档
    
    Returns:
        Dictionary mapping topic names to lists of document metadata
        Example: {
            'TikTok': [{doc1}, {doc2}],
            'AI工具': [{doc3}],
            ...
        }
    """
    all_docs = self.list_all_documents()
    
    by_topic = {}
    for doc in all_docs:
        topic = doc['topic']
        if topic not in by_topic:
            by_topic[topic] = []
        by_topic[topic].append(doc)
    
    return by_topic
```

**4. 添加辅助方法：**

```python
def _extract_topic_from_filename(self, filename: str) -> str:
    """
    从文件名提取主题
    
    Args:
        filename: 文件名（包含扩展名）
    
    Returns:
        主题名称（中文）
    """
    filename_lower = filename.lower()
    
    # 主题关键词映射
    if 'tiktok' in filename_lower:
        return 'TikTok'
    elif 'ai' in filename_lower or 'chatgpt' in filename_lower or 'gpt' in filename_lower:
        return 'AI工具'
    elif '财务' in filename_lower or 'finance' in filename_lower or '税' in filename_lower:
        return '财务'
    elif '收款' in filename_lower or 'payment' in filename_lower or '支付' in filename_lower:
        return '收款'
    elif '流量' in filename_lower or 'traffic' in filename_lower:
        return '流量'
    elif '广告' in filename_lower or 'ads' in filename_lower or 'advertising' in filename_lower:
        return '广告投放'
    elif 'facebook' in filename_lower or 'fb' in filename_lower:
        return 'Facebook'
    elif 'google' in filename_lower:
        return 'Google'
    else:
        return '其他'

def _format_timestamp(self, timestamp: float) -> str:
    """
    格式化时间戳为 ISO 格式
    
    Args:
        timestamp: Unix timestamp
    
    Returns:
        ISO format date string (YYYY-MM-DD)
    """
    from datetime import datetime
    return datetime.fromtimestamp(timestamp).strftime('%Y-%m-%d')
```

**5. 添加 get_document_stats() 方法：**

```python
def get_document_stats(self) -> Dict[str, Any]:
    """
    获取文档统计信息
    
    Returns:
        Dictionary with document counts and topics
    """
    counts = self.document_store.get_document_count()
    by_topic = self.list_documents_by_topic()
    
    return {
        'total_documents': counts['total'],
        'markdown_count': counts['markdown'],
        'excel_count': counts['excel'],
        'topics': {topic: len(docs) for topic, docs in by_topic.items()},
        'topic_list': sorted(by_topic.keys())
    }
```

**测试（添加到 tests/test_search_engine.py）：**

1. `test_list_all_documents()` - 返回所有 21 个文档
2. `test_document_metadata_structure()` - 每个文档包含 id/title/file_type/topic/size_bytes/modified_date
3. `test_list_documents_by_topic()` - 返回按主题分组的字典
4. `test_topic_extraction()` - 文件名包含 "tiktok" → topic="TikTok"
5. `test_document_stats()` - 返回统计信息（总数、主题数量）
6. `test_documents_sorted_by_name()` - 文档按文件名排序

使用真实的 DocumentStore（集成测试）。
  </action>
  
  <verify>
    <automated>pytest tests/test_search_engine.py::test_list_all_documents -v && pytest tests/test_search_engine.py::test_list_documents_by_topic -v</automated>
  </verify>
  
  <done>
    - SearchEngine.list_all_documents() 返回 21 个文档
    - 每个文档包含 id、title、file_type、topic、size_bytes、modified_date
    - SearchEngine.list_documents_by_topic() 返回按主题分组的字典
    - 主题包括：TikTok、AI工具、财务、收款、流量、广告投放、其他
    - SearchEngine.get_document_stats() 返回统计信息
    - 所有文档浏览测试通过（6 个测试）
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → SearchEngine | 用户请求文档列表 |
| SearchEngine → DocumentStore | 读取文件系统 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Spoofing | N/A | accept | 单用户系统，无需身份验证 |
| T-03-08 | Tampering | File system | accept | 用户控制自己的文件系统 |
| T-03-09 | Repudiation | N/A | accept | v1 无审计需求 |
| T-03-10 | Info Disclosure | Document list | accept | 用户自己的数据，本地访问 |
| T-03-11 | Denial of Service | File listing | accept | 文档数量有限（21 个），性能风险低 |
| T-03-12 | Elevation | N/A | accept | 单用户系统无权限级别 |
</threat_model>

<verification>
**集成测试：**
```bash
# 1. 文档列表测试
pytest tests/test_search_engine.py::test_list_all_documents -v

# 2. 主题分类测试
pytest tests/test_search_engine.py::test_list_documents_by_topic -v

# 3. 元数据结构测试
pytest tests/test_search_engine.py::test_document_metadata_structure -v

# 4. 全部文档浏览测试
pytest tests/test_search_engine.py -k "document" -v
```

**手动验证：**
```python
from src.search.search_engine import SearchEngine

engine = SearchEngine()

# 测试文档列表
docs = engine.list_all_documents()
print(f"Total documents: {len(docs)}")
for doc in docs[:3]:
    print(f"- {doc['title']} ({doc['topic']}, {doc['file_type']})")

# 测试主题分类
by_topic = engine.list_documents_by_topic()
print(f"\nTopics: {list(by_topic.keys())}")
for topic, docs in by_topic.items():
    print(f"- {topic}: {len(docs)} documents")

# 测试统计信息
stats = engine.get_document_stats()
print(f"\nStats: {stats}")

engine.close()
```

**预期输出：**
```
Total documents: 21
- document1 (TikTok, markdown)
- document2 (AI工具, markdown)
- document3 (财务, excel)

Topics: ['TikTok', 'AI工具', '财务', '收款', '流量', '广告投放', '其他']
- TikTok: 5 documents
- AI工具: 3 documents
- 财务: 2 documents
...

Stats: {
  'total_documents': 21,
  'markdown_count': 20,
  'excel_count': 1,
  'topics': {'TikTok': 5, 'AI工具': 3, ...},
  'topic_list': ['AI工具', 'TikTok', '其他', ...]
}
```
</verification>

<success_criteria>
- SearchEngine.list_all_documents() 返回 21 个文档
- 每个文档包含完整元数据（id、title、file_type、topic、size_bytes、modified_date、relative_path）
- SearchEngine.list_documents_by_topic() 返回按主题分组的字典
- 主题自动从文件名提取（TikTok、AI工具、财务、收款、流量、广告投放、其他）
- SearchEngine.get_document_stats() 返回统计信息
- 文档按文件名排序
- 所有文档浏览测试通过（6 个测试）
</success_criteria>

<output>
After completion, create `.planning/phases/03-semantic-search-implementation/03-02-SUMMARY.md`
</output>
